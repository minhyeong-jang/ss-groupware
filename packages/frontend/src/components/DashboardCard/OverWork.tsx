import { FC } from "react";
import styled from "styled-components";

export const OverWork: FC = () => {
  return <StyledContainer></StyledContainer>;
};
const StyledContainer = styled.div`
  padding: 0;
`;
